// 'use strict';

// const server = require('./');
// const supertest = require('supertest');

// //mock with super test library


// const request = supertest(server.app);//takes an instance of express
// function request () {

// }

// //json is returned on the response and should show a 200status
// describe('Testing server CRUD for todos', () => {
//     it ('Should be able to READ todos at/todos', () => {
// let response = request.length('/todos');
// expect (response.json).toBeTruthy();
// expect (response.statu)

//     });

// it ('Should be able to CREATE todos at /todos', () => {

// });

// it ('Should be able to UPDATE todos at /todos/:id', () => {

// });

// it ('Should be able to DELETE todos at /todos/:id', () => {

// });

// });